const HOST = "956246.cn";
const HOME = "http://" + HOST;
const BASE_URL = "http://" + HOST + ":8080";
const API_URL = "http://" + HOST + ":10888";

export default{
    HOST,
    HOME,
    BASE_URL,
    API_URL
}
